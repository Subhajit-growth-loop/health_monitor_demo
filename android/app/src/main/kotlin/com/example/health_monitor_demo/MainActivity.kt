package com.example.health_monitor_demo

import io.flutter.embedding.android.FlutterFragmentActivity

// Health Connect's permission flow uses an AndroidX ActivityResult contract,
// which requires the host to be a FragmentActivity.
class MainActivity : FlutterFragmentActivity()
